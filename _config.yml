# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# in the templates via {{ site.myvariable }}.
title: Ricky Dalexis
email: riccardi.dalexis@gmail.com
description: >- # this means to ignore newlines until "baseurl:"
  Mainly a brain dump about things I encounter while working. Otherwise, I probably
  would forget them...
url: "https://rickydalexis.com/" # the base hostname & protocol for your site
github_username:  rod7760

# Build settings
markdown: kramdown
remote_theme: jekyll/minima
plugins:
  - jekyll-remote-theme
  - jekyll-feed

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
exclude:
  - .gitignore
  - docker-compose.yml
  - Gemfile
  - Gemfile.lock
  - README.md
